---
import { brandAssets } from "../config/brand-assets";

interface Props {
  size?: number;
}

const { size = 32 } = Astro.props;
---

<img
  class="brand-mark"
  src={brandAssets.navigation}
  width={size}
  height={size}
  alt=""
  aria-hidden="true"
  decoding="async"
  draggable="false"
/>
